# macOS always reports light or dark, never no-preference — this file exists
# only so kitty has a defined fallback (and for Linux/GNOME, where the dark
# style being off is reported as no-preference).
include themes/xterm-dark.conf
