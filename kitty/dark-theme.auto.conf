# Applied automatically whenever macOS reports a dark appearance.
# These files must sit in the kitty config dir; their mere existence switches
# kitty into "follow the OS" mode, and their colors override kitty.conf.
#
# Swap this for themes/steel-dark.conf to match the fzf palette.
include themes/xterm-dark.conf
